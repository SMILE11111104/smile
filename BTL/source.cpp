#include "ngoai_tru.cpp"

int main(){
    PS *a[2];
    a[0] = new BN;
    cin >> (BN*) a[0];
    cout << (BN*) a[0];
    // a[1] = new NT;
    // cin >> (NT*) a[1];
    // cout << (NT*) a[1];
    return 0;
}